---
import { cn } from "@/lib/utils";

interface Props {
	class?: string;
	as?: keyof HTMLElementTagNameMap;
}

const { class: className, as: Tag = "div" } = Astro.props;
---

<Tag class={cn("mx-auto w-full max-w-site px-6 tablet:px-8", className)}>
	<slot />
</Tag>
